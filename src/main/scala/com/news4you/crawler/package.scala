package com.news4you

package object crawler {
 // type Crawler = Has[TodoRepository.Service]
/*
  def getContent(link:String): URIO[Crawler, TodoItem] =
    ZIO.accessM(_.get.(todoItemForm))

  def getById(id: TodoId): URIO[Crawler, Option[TodoItem]] =
    ZIO.accessM(_.get.getById(id))

  def getAll: URIO[Crawler, List[TodoItem]] =
    ZIO.accessM(_.get.getAll)

  def delete(id: TodoId): URIO[Crawler, Unit] =
    ZIO.accessM(_.get.delete(id))

  def deleteAll: URIO[Crawler, Unit] =
    ZIO.accessM(_.get.deleteAll)

  def update(
    id: TodoId,
    todoItemForm: TodoItemPatchForm
  ): URIO[Crawler, Option[TodoItem]] =
    ZIO.accessM(_.get.update(id, todoItemForm))*/

}
